import React from 'react';

import classes from './Providers.module.css';

const providers = () => (
    <div className={classes.Providers}>
        <h1>Let's Setup your<br />Provider Account!</h1>
        <p>Includes providers, non-physician providers, and labs/diagnostic services: Physicians, Hospitals, Health Practices, Podiatry, Acupuncture, Chiropractor, Physical Therapy, Dental, Home Health, Behavioral Health, Laboratory, etc.</p>
    </div>
);

export default providers;